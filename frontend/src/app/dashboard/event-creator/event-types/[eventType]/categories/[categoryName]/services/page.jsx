import React from 'react'

function CategoryServices({params}) {
  // api call to get services in that category
  const { categoryName } = params;
  return (
    <div>CategoryServices for eg cater services</div>
  )
}

export default CategoryServices